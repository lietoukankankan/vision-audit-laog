services:
  - type: web
    name: vision-audit-laog
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        value: AIzaSyBi32MrzhErq_2BMYyhUB8Olci0QwNAlz4
